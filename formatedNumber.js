// const inputField = document.querySelector("input");

// inputField.onkeyup = function () {
//   const removeCharacters = this.value.replace(/[^0-9.]/g, ""); // This removes alphabets and special characters

//   const removeDots = removeCharacters.replace(/\./g, ""); // This is to remove "DOT"

//   const formatedNumber = removeDots.replace(/\B(?=(\d{2})+(?!\d))/g, ","); // This is to
//   this.value = formatedNumber;

//   const formNumber = Number(formatedNumber);
//   formNumber;
// };

// export default inputField;
